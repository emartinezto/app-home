// Por defecto (cuando angular.json no aplica fileReplacements) usamos los valores
// de producción para evitar errores en builds que no sean dev.
export { environment } from './environment.production';
